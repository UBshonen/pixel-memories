/**
 * 조작 방식 설정.
 *
 * PHASE 10에서 두 방식을 실제 폰으로 비교하기 위한 임시 스위치다.
 * 방식이 정해지면 이 파일은 지운다.
 *
 * 이 파일은 Phaser를 import하지 않는다. 게임 쪽과 React 쪽 양쪽에서 쓴다.
 */

/**
 * 화면에 상하좌우 버튼을 띄울지.
 *
 * false여도 탭해서 걸어가기와 키보드는 그대로 동작한다.
 */
export const SHOW_DIRECTION_PAD = true;
